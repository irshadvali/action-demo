module.exports = {
  overrides: [
    {
      files: ['*.js', '*.jsx', '*.ts', '*.tsx'], // Specify the file types directly in the config
      rules: {
        // your rules here
      },
    },
  ],
};